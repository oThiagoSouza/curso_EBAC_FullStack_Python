function multiplica(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

console.log(multiplica(2, 4));

function dizOla(nome: string): string {
    return "Olá, " + nome;
}

console.log(dizOla("João"));
